# Social share hero image

## Changes
- Capture the homepage “Why this customer is at risk” panel exactly as it currently renders.
- Export a static PNG at 1200×630 when the existing proportions allow a clean crop.
- Save the PNG in the public site files and replace the current Open Graph and Twitter image URLs.

## Verification
- Inspect the exported PNG for clipping, overlap, and legibility.
- Confirm its pixel dimensions, run the relevant tests, and confirm a clean build.
- Do not publish; report the file and dimensions for review.
